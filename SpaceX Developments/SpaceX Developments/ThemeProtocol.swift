import SwiftUI

/**
 Protocol for themes
 */
protocol ThemeProtocol {
    var cardColor: Color { get }
    var shadowColor: Color { get }
    var listDividerColor: Color { get }
    var rocketBackground: Color { get }
}
